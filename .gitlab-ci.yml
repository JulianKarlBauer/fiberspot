stages:
  - plot

install:
    stage: plot
    image: "python:3.7"
    script:
        - python setup.py develop
        - python s001.py
        # - pip install pytest
        # - pytest --verbose
    artifacts:
        paths:
          - plots/
          - output/
        expire_in: 2 days




